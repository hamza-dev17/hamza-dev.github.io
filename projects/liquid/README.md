# Liquid

A fluid mirror simulation that reacts to live webcam motion and uses ping-pong render targets.

## Controls
- Move in front of the camera to push color into the fluid.

## Requirements
- Browser camera permission
- Three.js loaded from CDN

## Live Page
- `index.html`
